import { h, FunctionalComponent } from "preact"
import styled from "styled-components"

export type ${1:`(file-name-base)`}Props = {
  $2
}

export const $1: FunctionalComponent<$1Props> = ({
  $3
}) => $4
